# Autenticación de la app (NextAuth)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=genera_uno_con_openssl_rand_base64_32

# Base de datos (Postgres del VPS; en desarrollo, túnel SSH)
DATABASE_URL="postgresql://gestorlegal:TU_PASSWORD@127.0.0.1:5432/gestorlegal"

# API key que deben enviar los workflows de n8n en el header x-api-key
N8N_API_KEY=genera_una_clave_larga_y_secreta
