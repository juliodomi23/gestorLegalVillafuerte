# Copia este archivo a .env y rellena los valores reales (NO subir .env a git)
POSTGRES_DB=gestorlegal
POSTGRES_USER=gestorlegal
# Genera una contraseña fuerte, p.ej.: openssl rand -base64 24
POSTGRES_PASSWORD=cámbiame_por_una_contraseña_fuerte
